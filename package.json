{
  "name": "react-native-infinite-calendar",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "watch": "mkdir -p lib && babel -w src -d lib",
    "build": "mkdir -p lib && babel src -d lib",
    "clean": "rm -rf lib/"
  },
  "files": [
    "lib",
    "src"
  ],
  "dependencies": {
    "date-fns": "^1.27.2",
    "lodash": "^4.17.4"
  },
  "peerDependencies": {
    "react": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-eslint": "^7.1.1",
    "babel-jest": "18.0.0",
    "babel-preset-react-native": "1.9.2",
    "eslint": "^3.15.0",
    "eslint-plugin-react": "^6.9.0",
    "jest": "18.1.0",
    "react-test-renderer": "~15.4.0",
    "react": "^16.0.0-alpha.6",
    "react-native": "^0.45.1"
  },
  "jest": {
    "preset": "react-native"
  }
}
